package S2;

public class Brute {
    // Examine 4 points at a time and check whether they all lie on the same line segment

    // Print any such line segments to StdOut

    // Check whether the slopes between p->q, q->r, p->s are all equal

    // The points within each pattern must be given in default sorted order

    // The order of the patterns output must be in default sorted order
}
