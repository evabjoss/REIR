package S2;

public class Fast {

    // P is the origin - For each other point q, determine the slope it makes with p
    // Sort the points according to the slopes they make with p
    // Check if any 3 (or more) adjacent points in the sorted order have equal slopes with respect to p. -
    // If so, collinear

    // Same input as for B
    // Print out all line segments. The output must be sorted,

}
